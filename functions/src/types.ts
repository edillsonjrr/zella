// Tipos do domínio: fonte única em shared/dominio.ts (compartilhado com o
// frontend). Este arquivo só reexporta, pra que o resto das functions
// continue importando de './types'.
export * from '../../shared/dominio';
